[Unit]
Description=Cliphist Image Watcher
PartOf=graphical-session.target
After=graphical-session.target

[Service]
ExecStart=/usr/bin/sh -c 'wl-paste --type image --watch cliphist store'
Restart=always
RestartSec=2

[Install]
WantedBy=graphical-session.target
